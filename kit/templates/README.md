# Templates

Files installed by `fuckia init --apply`.

Directories:

- `project/` - root governance files.
- `github/` - PR template and workflow files.
- `docs/` - installed Fuckia documentation.

Rule: templates install governance only. Product code is never installed from this directory.

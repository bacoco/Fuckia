# Project Templates

Root-level governance templates for target repositories.

Files:

- `AGENTS.md`
- `CLAUDE.md`
- `fuckia.config.yaml`
- `root-readme.md` - installed as `README.md` only when the target repository has no README.

Agent-specific files are installed only when the selected mode enables that agent.

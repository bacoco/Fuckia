# Strict Mode

This directory contains local strict-mode checks and activation code.

Strict mode verifies installed governance files, generated skill headers, evidence language, PR template guardrails, Linear templates, GitHub workflows, and the end-of-work checkpoint.

# Install Scripts

This directory contains the Node-free installer used by the normal Claude/Codex installation path.

`agent-install.sh` installs the committed templates and generated skills from the Fuckia repository into a target repository. It does not require Node.js or npm.

# Scripts

Repository maintenance scripts.

Directories:

- `build/` - build-time helpers for generated local output.

Scripts may generate files under `dist/`. Generated `dist/` output is not committed.

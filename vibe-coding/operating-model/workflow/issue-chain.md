# Issue Chain

Use separate Linear issue types or labeled templates:

```text
spec -> plan -> plan-review -> implement -> code-review -> verify
```

Rules:

- planner is not the plan reviewer;
- implementer is not the code reviewer for risky work;
- plan-review blocks implementation;
- verification blocks Done;
- uncertainty escalates to human review.


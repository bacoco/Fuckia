# Required Test Fixtures

Fixtures must prove that each law blocks at least one bad path.

Required examples:

- root-level spec sprawl fails progressive disclosure validation;
- unsupported claim fails evidence validation;
- memory-only implementation fails source-of-truth validation;
- unauthorized parallel pipeline fails preservation validation;
- self-review on risky work fails authority validation;
- Done without real workflow receipt fails verification validation.


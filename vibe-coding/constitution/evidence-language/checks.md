# Checks

Required implementation:

- shared generated skill `evidence-language-guard`;
- deterministic validator `validate-evidence-language`;
- PR check for plans, reviews, receipts, prompts, and comments when available;
- allowlist for quoted examples and test fixtures;
- failure output with exact line and replacement format.


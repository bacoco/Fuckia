# Law 4 - Preserve Working Systems

An agent must preserve existing working behavior unless the active contract explicitly authorizes replacement.

The default is to preserve engines, routes, shells, hooks, stores, callbacks, queues, state machines, pipelines, public entry paths, and storage contracts.

No parallel pipeline is allowed without explicit authorization.


# Non-Goals

The first implementation slice must not:

- implement Claude marketplace publishing;
- implement Codex plugin packaging;
- write into an existing product repo;
- modify product source code;
- install strict mode by default;
- contact Linear APIs;
- require GitHub tokens;
- create autonomous AI review loops;
- create a dashboard;
- add unrelated repo cleanup.
- auto-rewrite the Fuckia repository from its own generator.
- commit local install output from ad hoc tests.

These are later phases after the CLI skeleton, doctor, and dry-run migration are verified.

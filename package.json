{
  "name": "fuckia",
  "version": "0.1.0",
  "private": false,
  "description": "Read-only first slice of the Fuckia Claude/Codex/Linear/GitHub collaboration kit.",
  "type": "commonjs",
  "bin": {
    "fuckia": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "vibe-coding"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/build/create-cli-wrapper.mjs",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "fuckia": "node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}

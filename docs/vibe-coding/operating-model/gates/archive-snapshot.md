# Archive Snapshot

Before or with merge, create/update:

```text
docs/linear-archive/<LINEAR-ID>/
  spec-snapshot.md
  contract.md
  verification-receipt.md
  links.md
```

The snapshot includes Linear URLs, PR URL, commit SHA, and final verification result.


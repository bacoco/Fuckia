# Blueprint

This directory contains the implementation blueprint.

Current file:

- `implementation-blueprint.md` - consolidated v1 blueprint.

Target split for implementation:

- repository layout;
- CLI commands;
- configuration schema;
- neutral skill schema;
- GitHub workflows;
- Linear templates;
- warning versus strict mode;
- pilot and fixtures.

The implementation must split these targets into focused files before coding.

